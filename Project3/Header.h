#pragma once
#include <iostream>
#include <vector>
#include <random>

using std::cout;
using std::cin;
using std::vector;

int DyA(vector<int>, vector<int>);
void siembra(vector<vector<int>>&);
bool criba(vector<vector<int>>&, vector<int>&, int);
int rndm(int, int);
void prnt(vector<int>&);
void leave();
int isz(vector<vector<int>>&);
